<Window x:Class="HyperNote.Controls.GoToSymbolDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Go to Symbol"
        Width="550" Height="320"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded"
        Deactivated="Window_Deactivated">
        
    <Border CornerRadius="8"
            Background="{DynamicResource App.Background}"
            BorderBrush="{DynamicResource App.Border}"
            BorderThickness="1"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Direction="270" Color="#000" Opacity="0.45"/>
        </Border.Effect>
        
        <DockPanel Margin="12">
            <!-- Search bar -->
            <Grid DockPanel.Dock="Top" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="@" VerticalAlignment="Center" Margin="8,0,12,0" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource App.Accent}"/>
                <TextBox x:Name="SearchBox"
                         Grid.Column="1"
                         Background="Transparent"
                         Foreground="{DynamicResource App.Foreground}"
                         BorderThickness="0,0,0,1"
                         BorderBrush="{DynamicResource App.Border}"
                         CaretBrush="{DynamicResource App.Foreground}"
                         FontSize="14"
                         VerticalContentAlignment="Center"
                         Padding="2,4"
                         TextChanged="SearchBox_TextChanged"
                         PreviewKeyDown="SearchBox_PreviewKeyDown"/>
            </Grid>
            
            <!-- Matching Symbols List -->
            <ListBox x:Name="SymbolResults"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     MouseDoubleClick="SymbolResults_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="2,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <!-- Symbol Label and Level Indent -->
                            <TextBlock Grid.Column="0" Text="{Binding DisplayName}"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource App.Foreground}"
                                       FontSize="12"/>
                            <!-- Line number hint -->
                            <TextBlock Grid.Column="1" Text="{Binding LineHint}"
                                       Foreground="{DynamicResource Editor.LineNumbers}"
                                       FontSize="11" Margin="10,0,4,0"
                                       HorizontalAlignment="Right"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="8,4"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource App.Border}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource App.Accent}"/>
                                <Setter Property="Foreground" Value="{DynamicResource App.Background}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </DockPanel>
    </Border>
</Window>
