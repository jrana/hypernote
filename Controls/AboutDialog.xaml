<Window x:Class="HyperNote.Controls.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="About HyperNote"
        Width="400" Height="260"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Deactivated="Window_Deactivated">
        
    <Border CornerRadius="12"
            Background="{DynamicResource App.Background}"
            BorderBrush="{DynamicResource App.Border}"
            BorderThickness="1"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="3" Direction="270" Color="#000" Opacity="0.45"/>
        </Border.Effect>
        
        <Grid Margin="20,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                <ui:SymbolIcon Symbol="DocumentText24" FontSize="36" Foreground="{DynamicResource App.Accent}" Margin="0,0,12,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="HyperNote" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource App.Foreground}"/>
                    <TextBlock Text="Version 1.2.0" FontSize="11" Foreground="{DynamicResource Editor.LineNumbers}"/>
                </StackPanel>
            </StackPanel>
            
            <!-- Content -->
            <StackPanel Grid.Row="1">
                <TextBlock Text="A lightweight, modern, and feature-rich text editor built with WPF and Fluent Design."
                           Foreground="{DynamicResource App.Foreground}"
                           FontSize="12"
                           TextWrapping="Wrap"
                           Margin="0,0,0,16"
                           LineHeight="16"/>
                           
                <TextBlock Foreground="{DynamicResource App.Foreground}" FontSize="12">
                    <Run Text="Developed by the HyperNote Team."/>
                </TextBlock>
                <TextBlock Margin="0,4,0,0" FontSize="12">
                    <Hyperlink NavigateUri="https://github.com/google-deepmind" RequestNavigate="Hyperlink_RequestNavigate">
                        GitHub Project
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
            
            <!-- Bottom Button -->
            <Button Grid.Row="2" Content="Close" Width="90" Height="28" HorizontalAlignment="Right" IsCancel="True" Click="Close_Click"/>
        </Grid>
    </Border>
</Window>
